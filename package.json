{
  "name": "mongoDb",
  "version": "1.0.0",
  "description": "study mongoDb",
  "main": "index.js",
  "scripts": {
    "run:dev": "cross-env NODE_ENV=dev node --watch src/main.js  --enable-turbo-mode",
    "run:prod": "cross-env NODE_ENV=prod node --watch src/main.js  --enable-turbo-mode"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "cross-env": "^10.1.0",
    "dotenv": "^18.0.1",
    "env": "^0.0.2",
    "express": "^5.2.1",
    "mongodb": "^7.6.0"
  }
}